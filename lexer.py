# We use Jay Conrod's IMP language

ASSIGN = ":="
EQ = "="
LT = "<"
GT = ">"
LTE = "<="
GTE = ">="
IF = "if"
ELSE = "else"
WHILE = "while"

keywords = [
    ASSIGN,
    EQ,
    LT,
    GT,
    LTE,
    GTE,
    IF,
    ELSE,
    WHILE,
]


def lex():
    pass
